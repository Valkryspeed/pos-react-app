<Application x:Class="Application"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:POS_JAVISH_WPF"
             xmlns:helpers="clr-namespace:POS_JAVISH_WPF.Helpers"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Startup="OnAppStartup">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="Blue" SecondaryColor="Indigo" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <helpers:PosNegConverter x:Key="PosNegConverter" />
            
            <!-- Enterprise POS Colors -->
            <SolidColorBrush x:Key="PosBackground" Color="#0F172A"/>
            <SolidColorBrush x:Key="PosPanel" Color="#1E293B"/>
            <SolidColorBrush x:Key="PosAccent" Color="#3B82F6"/>
            <SolidColorBrush x:Key="PosText" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="PosTextMuted" Color="#94A3B8"/>
            <SolidColorBrush x:Key="PosBorder" Color="#334155"/>
            <SolidColorBrush x:Key="PosSuccess" Color="#10B981"/>
            <SolidColorBrush x:Key="PosWarning" Color="#F59E0B"/>
            <SolidColorBrush x:Key="PosError" Color="#EF4444"/>

            <!-- Global Styles -->
            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
                <Setter Property="Foreground" Value="{StaticResource PosText}"/>
            </Style>

            <Style TargetType="materialDesign:Card">
                <Setter Property="Background" Value="{StaticResource PosPanel}"/>
                <Setter Property="Foreground" Value="{StaticResource PosText}"/>
                <Setter Property="UniformCornerRadius" Value="8"/>
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
            </Style>

            <!-- DataGrid Custom Style -->
            <Style TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
                <Setter Property="Background" Value="{StaticResource PosPanel}"/>
                <Setter Property="RowBackground" Value="{StaticResource PosPanel}"/>
                <Setter Property="Foreground" Value="{StaticResource PosText}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource PosBorder}"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
            </Style>
            
            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                <Setter Property="Background" Value="{StaticResource PosBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource PosTextMuted}"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="10"/>
            </Style>
            
            <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                <Setter Property="Padding" Value="10"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource PosAccent}"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>