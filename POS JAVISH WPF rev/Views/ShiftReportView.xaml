<UserControl x:Class="Views.ShiftReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Laporan Shift Kasir" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource PosText}"/>

        <!-- Filters -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,15" Padding="15" Background="{StaticResource PosPanel}" UniformCornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DatePicker x:Name="StartDatePicker" Grid.Column="0" Margin="0,0,10,0"
                            materialDesign:HintAssist.Hint="Mulai Tanggal" Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>
                <DatePicker x:Name="EndDatePicker" Grid.Column="1" Margin="0,0,10,0"
                            materialDesign:HintAssist.Hint="Sampai Tanggal" Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>
                <ComboBox x:Name="UserCombo" Grid.Column="2" Margin="0,0,10,0"
                          materialDesign:HintAssist.Hint="Filter Kasir" Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          DisplayMemberPath="Username" SelectedValuePath="Id"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button x:Name="BtnFilter" Content="FILTER" Click="BtnFilter_Click" Margin="0,0,10,0"
                            Background="{StaticResource PosAccent}" BorderBrush="{StaticResource PosAccent}"/>
                    <Button x:Name="BtnExport" Content="EXPORT CSV" Click="BtnExport_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="{StaticResource PosSuccess}" Foreground="{StaticResource PosSuccess}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Report Table -->
        <Border Grid.Row="2" Background="{StaticResource PosPanel}" CornerRadius="8">
            <DataGrid x:Name="ShiftGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mulai" Binding="{Binding ShiftStart, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>
                    <DataGridTextColumn Header="Selesai" Binding="{Binding ShiftEnd, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>
                    <DataGridTextColumn Header="Kasir" Binding="{Binding Username}" Width="120"/>
                    <DataGridTextColumn Header="Modal" Binding="{Binding ModalAwal, StringFormat=C0}" Width="110"/>
                    <DataGridTextColumn Header="Penjualan" Binding="{Binding TotalPenjualan, StringFormat=C0}" Width="110"/>
                    <DataGridTextColumn Header="Cash" Binding="{Binding TotalCash, StringFormat=C0}" Width="110"/>
                    <DataGridTextColumn Header="Uang Fisik" Binding="{Binding UangFisik, StringFormat=C0}" Width="110"/>
                    <DataGridTextColumn Header="Selisih" Binding="{Binding Selisih, StringFormat=C0}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMinus}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource PosError}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsPlus}" Value="True">
                                        <Setter Property="Foreground" Value="Yellow"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsOk}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource PosSuccess}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                    <DataGridTemplateColumn Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignIconButton}" Click="PrintShift_Click" ToolTip="Cetak Laporan">
                                    <materialDesign:PackIcon Kind="Printer"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>