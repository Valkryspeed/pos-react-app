<UserControl x:Class="Views.ProdukView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Search -->
        <Border Grid.Row="0" Margin="0,0,0,20" Background="{StaticResource PosPanel}" Padding="20" CornerRadius="8" Effect="{DynamicResource MaterialDesignShadowDepth1}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="SearchBox" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Cari Produk (Nama / Kode)"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Width="400" HorizontalAlignment="Left"
                         TextChanged="SearchBox_TextChanged"
                         Foreground="{StaticResource PosText}"
                         CaretBrush="{StaticResource PosText}"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnExcelExport" Content="Excel Export" Style="{StaticResource MaterialDesignOutlinedButton}" Click="ExportExcel_Click" Margin="0,0,10,0" BorderBrush="{StaticResource PosAccent}" Foreground="{StaticResource PosAccent}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileExcelOutline" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="EXPORT"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button x:Name="BtnExcelImport" Content="Excel Import" Style="{StaticResource MaterialDesignOutlinedButton}" Click="ImportExcel_Click" Margin="0,0,10,0" BorderBrush="{StaticResource PosWarning}" Foreground="{StaticResource PosWarning}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileUploadOutline" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="IMPORT"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button Content="Tambah Produk" Style="{StaticResource MaterialDesignRaisedButton}" Click="AddProduct_Click" Background="{StaticResource PosAccent}" BorderBrush="{StaticResource PosAccent}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="TAMBAH PRODUK"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Product List -->
        <Border Grid.Row="1" Background="{StaticResource PosPanel}" CornerRadius="8" Effect="{DynamicResource MaterialDesignShadowDepth1}">
            <DataGrid x:Name="ProductGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      CanUserSortColumns="True"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      Background="Transparent" BorderThickness="0"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8" Margin="10,10,0,-10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kode" Binding="{Binding KodeBarang}" Width="150"/>
                    <DataGridTextColumn Header="Nama Barang" Binding="{Binding NamaBarang}" Width="*"/>
                    <DataGridTextColumn Header="Harga Modal" Binding="{Binding HargaModal, StringFormat=C0}"/>
                    <DataGridTextColumn Header="Harga Jual" Binding="{Binding HargaJual, StringFormat=C0}" FontWeight="Bold">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource PosSuccess}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Satuan" Binding="{Binding Satuan}"/>
                    <DataGridTextColumn Header="Stok" Binding="{Binding Stok}" FontWeight="Bold"/>
                    <DataGridTemplateColumn Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Edit" Click="EditProduct_Click" Foreground="{StaticResource PosAccent}">
                                        <materialDesign:PackIcon Kind="Pencil"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Hapus" Click="DeleteProduct_Click" Foreground="{StaticResource PosError}">
                                        <materialDesign:PackIcon Kind="Delete"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
