<UserControl x:Class="Views.ExpenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Manajemen Pengeluaran" Style="{StaticResource MaterialDesignHeadline5TextBlock}" VerticalAlignment="Center" Foreground="{StaticResource PosText}"/>
            <Button Content="TAMBAH PENGELUARAN" Margin="20,0,0,0" Style="{StaticResource MaterialDesignRaisedButton}" Background="{StaticResource PosError}" Click="AddBtn_Click"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="1" UniformCornerRadius="8" Background="{StaticResource PosPanel}">
            <DataGrid x:Name="ExpenseGrid" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single"
                      Background="Transparent" BorderThickness="0" materialDesign:DataGridAssist.CellPadding="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tanggal" Binding="{Binding Tanggal, StringFormat='dd/MM/yyyy HH:mm'}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Kategori" Binding="{Binding Kategori}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Keterangan" Binding="{Binding Keterangan}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Jumlah" Binding="{Binding Jumlah, StringFormat=C0}" Width="150" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignIconButton}" Click="DeleteBtn_Click" ToolTip="Hapus">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="{StaticResource PosError}"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Margin="0,20,0,0" Padding="15" Background="{StaticResource PosBackground}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Total Pengeluaran: " VerticalAlignment="Center" FontSize="16" Foreground="{StaticResource PosTextMuted}"/>
                <TextBlock x:Name="TotalExpenseText" Text="Rp 0" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PosError}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
